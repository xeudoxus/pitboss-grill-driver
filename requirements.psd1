# PowerShell module dependencies for build and test automation.
# For use with tools like Dependabot and `Install-Module`.
@{
    # ModuleName = 'Version'
    'Pester' = '*' # Install the latest stable version for cross-platform compatibility
}